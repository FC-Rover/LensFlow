server {
    listen 8088;

    location / {
        add_header Access-Control-Allow-Origin *;
        root /var/snap/lens-flow/common/var/www/html/stream;
    }
}

types {
    application/dash+xml mpd;
}
